# Implementation Plan - Ads, Coupons & Curated Library

Enhance the platform with professional advertisement support, coupon management, and a robust curated library for Ghanaian and global literature.

## User Review Required

> [!IMPORTANT]
> - Competition images will be served from the `/uploads/images` directory.
> - The price per student (10 Cedis) is already globally configured in the system settings; we will ensure it is visually emphasized in the UI.

## Proposed Changes

---

### Backend Components

#### [MODIFY] [auth_service.py](file:///c:/Users/HP/Desktop/bisame_osuani/backend/app/services/auth_service.py)
- Add `image_url` column migration check in `_ensure_tables`.
- Implement `get_unused_access_codes()` to retrieve codes that haven't been redeemed.
- implement `update_competition_image(comp_id, image_url)` to persist image paths.

#### [MODIFY] [admin.py](file:///c:/Users/HP/Desktop/bisame_osuani/backend/app/routes/admin.py)
- Add `POST /competitions/{comp_id}/upload-image` endpoint (multipart/form-data).
- Add `GET /coupons/inventory` endpoint to fetch the unused codes list.

---

### Frontend Components

#### [MODIFY] [api.ts](file:///c:/Users/HP/Desktop/bisame_osuani/frontend/src/services/api.ts)
- Add `uploadCompetitionImage(compId, file)` and `getCouponInventory()` methods.
- Update `Competition` interface to include `image_url`.

#### [MODIFY] [AdminDashboard.tsx](file:///c:/Users/HP/Desktop/bisame_osuani/frontend/src/components/AdminDashboard.tsx)
- **Coupon Vault**: Add a new UI section to display unused access codes with a "Copy to Clipboard" feature.
- **Image Upload**: Add an upload button to competition cards for advertisement images.

#### [MODIFY] [ResourceFetcher.tsx](file:///c:/Users/HP/Desktop/bisame_osuani/frontend/src/components/ResourceFetcher.tsx)
- **Curated Content**: Replace the generic search with fixed categories (Ghanaian Classics, Self-Development, Financial Literacy).
- **Hardcoded Staples**: Include a pre-populated list of must-read Ghanaian authors (Ayi Kwei Armah, Yaa Gyasi, etc.) and global major topics to ensure the library never feels empty.
- **Improved Retrieval**: Fix the Google Books integration to ensure images and links load reliably.

#### [MODIFY] [CompetitionPortal.tsx](file:///c:/Users/HP/Desktop/bisame_osuani/frontend/src/components/CompetitionPortal.tsx)
- Render the competition's advertisement image if available to make it look like a professional announcement.

---

## Verification Plan

### Automated Tests
- Test image upload via custom script.
- Verify `GET /api/admin/coupons/inventory` returns the correct list of unused codes.

### Manual Verification
- Upload an image in the Admin Dashboard and verify it displays for students in the portal.
- Generate new codes and verify they appear in the "Coupon Vault".
